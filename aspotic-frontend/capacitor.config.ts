import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'aspotic-front',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
